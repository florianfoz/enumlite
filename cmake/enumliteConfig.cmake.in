@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@ENUMLITE_BUILD_QT@)
    find_dependency(Qt6 COMPONENTS Core)
endif()

include(
    "${CMAKE_CURRENT_LIST_DIR}/enumliteTargets.cmake"
)

check_required_components(enumlite)